# Billing Module Context

Last reviewed: 2026-08-24
Status: Canonical module runtime; backend aliases are retired and the active imported-GCash page URL is documented below.

## Purpose and current scope

- Manage prepaid/postpaid plans and MikroTik profile bindings.
- Generate monthly billing entries, proration, balances, advances, and credit-limit state.
- Record, import, export, normalize, number, adjust, and delete payment entries.
- Produce payment history, breakdowns, receipts, account statements, and billing statements.
- Review customer payment confirmations and unmatched imports.
- Persist customer GCash proof submissions in JSON or MySQL, detect reused references and exact screenshot hashes, bind each imported transaction to one immutable allocation group of up to three distinct customer/current-month rows, and post every ledger credit/service restoration only after explicit Admin approval.
- Extract amount, reference, date/time, recipient, and transaction status from submitted GCash screenshots with local server-side Tesseract OCR, optionally supplement incomplete/low-confidence reads through an explicitly enabled OpenAI-compatible Vision AI endpoint, and retain field provenance plus normalized analysis as review metadata without allowing either analyzer to approve a payment.
- Import official password-protected GCash Transaction History PDFs and match pending proofs against credited reference/amount/date/recipient rows before Admin approval is allowed.
- Reconcile Payment History Excel imports and official GCash PDF imports in either order against normalized references so exact one-to-three-account allocations bind once, existing ledger matches are linked without reinsertion, and amount/date/customer conflicts remain unposted with a current-customer suggestion for Admin review.
- Keep manually entered Admin GCash payments visible as Pending without affecting billing, then convert the same ledger entry to an effective posted payment only after Admin binds an exact unassigned imported incoming credit with the same amount and calendar date; an exact same-reference match is surfaced directly on the imported row as **Bind Pending**.
- Recover fixed-width numeric GCash references whose leading zeros were removed by Excel, without applying fuzzy matching to alphanumeric references or weakening amount/date/customer/payment-type guards.
- Drive disconnection, keep-active, reconnection, and disconnected billing-policy workflows.

## Backend and APIs

- `backend/index.js` is the lazy module descriptor loaded by `server.js` through `core/runtime/module-loader`.
- Fourteen runtime entries plus internal complimentary/reconnection helpers, the GCash PDF/parser/history stores, `gcash-screenshot-parser.js`, and the provider-neutral `gcash-vision-ai.js` adapter live under `backend/`: plans, payments, records, confirmations, disconnections, scheduler, numbering, normalization, balance, refresh, profile, first-bill transfer, and queue/store helpers.
- The former thirteen repository-root backend shims were retired in Phase 11; consumers use canonical Billing paths or the module descriptor.
- `/api/plans`: plan CRUD through `backend/plans.js`.
- `/api/payments`: payment listing, entry, import/export, delete, and payment-link flows through `backend/payments.js`.
- `GET /api/payments/reference-availability` checks a manual reference against every branch Payment History/pending entry and imported GCash transaction. Comparison ignores case, spaces, and hyphens and treats numeric leading-zero variants as the same reference. `POST /api/payments/:accountNumber` repeats the same check immediately before JSON or relational insertion and returns `PAYMENT_REFERENCE_ALREADY_USED` on conflict; available imported GCash references must be used from GCash Transactions instead of being re-entered in Payments.
- Manual `POST /api/payments/:accountNumber` entries whose method is GCash are stored as `pending_gcash_verification`; they retain the entered customer, amount, date, and reference for review but do not re-enable service, extend prepaid expiry, change collected totals, or affect canonical balances. `GET /api/payments/gcash-pending` lists them for the Admin branch. `GET /api/payments/gcash-pending/:accountNumber/:entryId/options` exposes only unassigned/unlocked incoming official credits with the exact amount and date, and `POST .../bind` requires explicit confirmation before atomically converting the same entry ID to Approved, replacing its reference/date with the imported proof and recording the official transaction time. The imported-history read model marks a unique same-reference/customer/amount/date pending entry as `pending_match` and returns only the identifiers needed to launch that binding flow. The imported assignment is permanently finalized against that entry ID; exact retries are idempotent, no second payment is inserted, failed pre-write claims are released, and post-write finalization remains retryable.
- `/api/payment-records`: account payment history and adjustments. Every record includes version 2 of the canonical backend-only `billingSummary` read model (`available`, `rows`, `context`, `currentCycle`, `nextCycleDate`, `billingStatus`, `dueDate`, `endingBalance`, `balance`, `advance`, and `reconciliation`). `nextCycleDate` is the next first day for prepaid and next month-end for postpaid.
- `PATCH /api/payment-records/:accountNumber/breakdown-adjustment` accepts an Admin-confirmed `planChange` with an effective month and required reason. The backend resolves the selected plan from the canonical plan store, owns plan amounts/categories, records the actor/time and previous plan, rejects direct client-authored `planChanges`, recalculates open cycles, protects paid cycles, and returns the subscriber synchronization result.
- The same adjustment endpoint accepts only an audited `complimentaryAccount` action with a current/future effective month, optional inclusive end month, required reason/confirmation, and Keep or current-month Write Off treatment for an existing balance. Direct browser-authored complimentary periods are rejected. Ending a period selects a later resume month so free months are never back-billed.
- `/api/payment-records` automatically assigns the oldest eligible Admin-approved queued referrals to the subscriber's latest generated unpaid billing month. A referral is eligible when its optional `applyFromMonth` is blank or is not later than that billing month. It applies at most two per month, never exceeds the remaining monthly charge, and carries every excess or future-scheduled referral forward without a queue limit. The adjustment endpoint accepts reasoned `referralApplication` reversals; manual application and browser-authored amounts are rejected.
- `/api/payment-records/reconciliation/report`: Admin branch-wide recalculation report for duplicate cycles, missing calculated charges, invalid advances/carry-over, missing current cycles, and ending-balance mismatches.
- Payment confirmation routers are mounted at shared confirmation queue paths.
- `POST /api/payment-confirmations/gcash-history/import` accepts an Admin-only raw PDF up to 8 MB plus its one-use password, parses every valid official transaction row, and stores normalized branch-scoped history without retaining the password or PDF bytes. Every normalized reference already present in the branch or earlier in the same upload is skipped without overwriting its stored details or stopping other new rows; the response reports imported, skipped-reference, conflicting-skipped, duplicate-file, batch-recorded, and Payment History reconciliation results. After the PDF write, the server scans canonical branch payment entries: an exact GCash reference/total/date/current-customer allocation is claimed and finalized against the existing entry IDs, while non-exact rows remain unbound. A repeated file with no new references is successful and does not add another batch record. `GET /api/payment-confirmations/gcash-history` returns the branch import/history view, including mapped recipient labels, each sanitized allocation when claimed, and a derived `paymentHistoryMatch` suggestion for unresolved same-reference ledger rows. Queue reads include `gcashMatch`; GCash approval returns `GCASH_HISTORY_MATCH_REQUIRED` unless reference, incoming credit, exact amount, calendar date, and configured merchant recipient all match. Sender/customer-mobile mismatches remain visible and require human ownership verification.
- `GET /api/payment-confirmations/gcash-history` accepts an optional validated `month=YYYY-MM`. A month request returns every official row in that month, the branch-wide total, the selected-month total, and descending `availableMonths`; the unfiltered response retains its previous limit behavior. Month selection is read-only and does not change reconciliation, assignments, remarks, or stored transactions.
- `POST /api/payment-confirmations/gcash-history/:reference/post-payment` is Admin-only and provides the explicit direct-import workflow. It accepts one to three distinct branch-account allocations with positive amounts plus the read-only imported amount and assignment confirmation; the browser does not select billing months. The server derives each account's canonical current cycle, payment status, and ending balance from the unchanged version-2 Payment Breakdown summary. Unpaid allocations remain capped by the positive amount due, while paid/settled or zero-balance current-cycle accounts accept the imported credit as an advance payment. Missing current cycles/balances, duplicate accounts, imported rows without an official transaction timestamp, and totals that do not exactly equal the credit are rejected. A newly posted ledger entry records the official imported GCash calendar date and a hidden `GCASH_RECEIVED_AT` fingerprint timestamp for display, while its separate `recordedAt` and immutable assignment `postedAt` retain the actual Admin posting time for audit. It writes the whole group atomically in one JSON replacement or one MySQL transaction and finalizes one immutable assignment containing every current-month ledger entry. Historical breakdown rows and the canonical breakdown calculation are not edited. Duplicate ledger references, partial groups, amount changes, wrong-account reuse, and reassignments fail closed; a retry of the identical completed group returns the original entries. The legacy single-account request shape remains supported, but any supplied billing month is ignored in favor of the server-derived current month.
- `POST /api/payments/import-excel` normalizes each GCash reference against the branch's official imported history before writing. One to three distinct workbook customer rows may share one reference only when their total equals the incoming credit and every payment date matches the official transaction date. The server claims the official allocation before the JSON/MySQL ledger write and finalizes it with the imported entry IDs afterward. An exact payment already in Payment History is linked to the official row and skipped instead of inserted again; an already-posted identical allocation is idempotently skipped. A final canonical-ledger reconciliation runs after the Excel write so a concurrent or just-finished PDF import cannot leave an exact match unbound. Reserved, wrong-account, amount, date, direction, partial-group, and reused-reference conflicts do not write payments and are reported for Admin review. Manual binding of an unmatched imported row applies the same reference guard after the Admin selects the customer. Payment Breakdown calculations are unchanged.
- `GET /api/payments/gcash-bindings` returns the authenticated Admin branch's posted official-GCash payment entry IDs for Payment History action locking. `GET /api/payments/:accountNumber/:entryId/edit-bind-options` and `PUT /api/payments/:accountNumber/:entryId/edit-bind` provide an Admin-only correction path only while one imported Cash/GCash Payment History entry is still unbound. The option read returns unassigned incoming credits whose official amount and calendar date exactly equal the locked ledger entry. Save requires explicit confirmation and a current branch customer, moves the same entry ID between customer ledgers in one JSON replacement or MySQL transaction, and keeps amount/date/OR/original recorder locked. Once the official transaction assignment is posted, both edit APIs reject the entry with `PAYMENT_HISTORY_GCASH_BINDING_LOCKED`; posted references cannot be reassigned through Payment History. Multi-account GCash allocation groups and audited reconnection payments remain blocked from partial editing. No second payment is inserted; failed ledger writes release a newly created claim, while a post-write binding failure remains retryable from the updated account. Payment Breakdown formulas and historical billing rows are unchanged.
- GCash reference lookup is exact-first. When both sides are digits only, a unique official reference may also match the same numeric value after leading zeros are removed; multiple official candidates fail closed. The fallback still requires exact incoming-credit total, calendar date, one-to-three distinct current customers, collected-payment direction, GCash payment type, and existing payment IDs. Existing short ledger references are linked without rewriting financial history, while future Excel rows matched through this fallback store the official padded reference and a corresponding fingerprint. `GET /api/payment-confirmations/gcash-history` runs the same idempotent reconciliation before returning rows so safe legacy matches repair on the next GCash Transactions refresh.
- `PUT /api/payment-confirmations/gcash-history/:reference/remark` is Admin-only and updates only the imported row's classification metadata. Allowed categories are Expense — Unclassified, Operating Expense, Transfer, Refund, and Personal/Other; each save records the Admin identity and timestamp without changing the original reference, direction, amount, description, date, recipient, or allocation.
- `POST /api/payment-confirmations/gcash-history/:reference/lock-posting` is Admin-only and marks one unassigned incoming credit **Not for Posting** with a required free-text remark plus Admin/time audit. `POST /api/payment-confirmations/gcash-history/:reference/unlock-posting` restores posting eligibility while retaining the complete lock/unlock audit. A locked reference remains in official history and duplicate protection, but the shared claim layer rejects direct Bind & Post, customer-proof approval, Payment History Excel reconciliation, automatic existing-ledger binding, and Edit & Bind with `GCASH_TRANSACTION_POSTING_LOCKED`. Debit rows and assigned/posted credits cannot use this workflow.
- GCash approval requires an explicit Admin customer-allocation confirmation, locks the originally submitted amount/reference, blocks reliable OCR contradictions, and atomically claims the imported reference for that submission/account before ledger posting. A conflicting claim returns `GCASH_TRANSACTION_ALREADY_ASSIGNED`; successful posting finalizes the allocation with the payment entry and reviewer audit.
- `/api/disconnections`: queue and service-policy decisions. `POST /api/disconnections/:accountNumber/reconnect` preserves the existing cycle for continue-billing disconnections; a stopped-billing account instead requires an Admin-confirmed, reasoned settlement with Keep, Write Off, or 2–24 month Installment treatment for the captured previous balance, Prorated Now, Full Month Now, or Next Regular Cycle for new billing, and Immediate or After Required Payment service activation. Both immediate-charge choices are rejected when the current month already has a generated regular bill.
- `/api/billing/run-once`: shared guarded billing trigger in `server.js`.
- Statement PDF and payment receipt handlers partly live in shared `server.js`.

All API prefixes and response contracts remain unchanged by the physical migration.

## Frontend entry points

- Canonical browser implementations live under `web/`, including eleven HTML entry points plus their root, `css/`, and `js/` assets.
- Existing root URLs such as `/plans.html`, `/payments.html`, `/quick-payment.html`, `/payment-receipt`, `/billing-statement.html`, and `/account-statement.html` are preserved through the configured module web root.
- `/payments.js` and `/plans.js` resolve from the Billing web root through shared static composition.
- Billing modal close controls on Payments, Payment Breakdown, Disconnections, and GCash Transactions use the shared Tabler outline-secondary icon-button contract with real `ti-x` icons.
- Selecting a subscriber name or row on `/payments.html` opens a Payment Breakdown modal. The modal hides Type, Billing Cycle, and the plan/amount/source metadata beneath each Bill, uses nearly the full desktop viewport (up to 1760px wide), and redistributes a compact fixed layout across the other 10 columns with wrapped, whole-word table headers; narrow screens retain horizontal scrolling for readability. **Add Payment** opens the existing transaction form with that subscriber locked, then refreshes the canonical breakdown after a successful payment. The standalone **New Transaction** form replaces its long customer dropdown with an accessible autocomplete: Admin can search by client name or account number, suggestions show only client name and canonical amount due, mouse/keyboard selection stores the unchanged account-number payload and keeps existing amount autofill. It also retains an **Open Full Page** link to `/payment-breakdown.html` for adjustment, plan-change, disconnection, and other full-page controls. The full Payment Breakdown page continues to show Type, Billing Cycle, and Bill metadata.
- The Payments **New Transaction** reference field checks availability while typing and again on submit. A reused normalized reference shows an inline error and cannot be submitted; the backend remains authoritative if the browser check is skipped or becomes stale.
- `web/js/payment-breakdown-table.js` and `web/css/payment-breakdown-table.css` are shared by the Payments modal and the full Payment Breakdown page so table markup, status styling, payment stacks, and currency presentation do not diverge.
- The full Payment Breakdown **Edit first bill** and **Change plan** buttons open separate Tabler modals instead of expanding inline toolbars. The existing field IDs, backend requests, calculations, and validation are unchanged. The plan modal accepts current, future, or past effective months back to activation, requires a reason and explicit paid-bill confirmation, filters choices to the subscriber's current prepaid/postpaid category, and displays audited plan history with scheduled/active/history/pending-sync state and any debit/credit adjustment.
- `backend/first-bill-adjustment-transfer.js` owns the branch/account transfer format for first-bill Previous Balance and Advance. Customer-focused export/import recognizes the canonical sheet plus legacy camelCase/snake_case fields, writes only missing values, and preserves newer first-bill fields, plan changes, complimentary periods, referrals, and update audit metadata.
- The full Payment Breakdown page shows an **Approved Referral Queue** with every approved or requeued customer referral still waiting for automatic application, including its locked amount and optional earliest month. This queue is read-only and never changes the balance early. The row-level referral control lists only discounts already applied to the selected month and supports reasoned reversal. Approval and scheduling changes remain in `referrals.html`; there is no manual Apply action. Arbitrary first-bill/monthly referral entry is no longer exposed, while existing legacy adjustment values remain readable and are preserved when unrelated fields are edited.
- The full Payment Breakdown page also provides a Tabler **Complimentary Account** modal. Admins choose when the exemption starts/ends, whether an existing balance remains or is written off, and a required audit reason. Payments and the shared breakdown table show Complimentary badges/status, the Billing Cycle shows the free period and resume date, and the actual subscriber plan/history remain unchanged.
- For a stopped-billing disconnection, the full Payment Breakdown **Reconnect** action opens a Tabler settlement modal instead of directly clearing the disconnection. It previews the separate previous-balance treatment, prorated/full-month immediate charge or next-cycle start, next regular bill, and activation rule; it blocks both same-month immediate charges when a regular charge already exists and requires an audit reason and confirmation. A payment-gated request remains disabled and displays the remaining activation amount until collected payments reach the threshold.
- Protected Billing pages retain the shared admin/customer authentication redirects.
- The GCash Transactions page is always available to authenticated Admin users and is no longer controlled by deployment feature settings. The unused standalone Payment Queue History page, script, stylesheet, navigation item, and routes are retired; removing that UI does not delete proof, imported transaction, allocation, payment, or audit records.
- `/gcash-transaction.html` provides **Import PDF** and renders every official statement row in a compact eight-column table with date/time, reference, description, sender, amount/direction, recipient, Match Use, and Actions. Incoming credits are organized by month into **Credits** and **Not for Posting**: Credits offers status/search filtering plus the existing Bind & Post, Bind Pending, and Remark & Lock actions, while locked credits appear only in Not for Posting with their required remark and Admin/time audit and expose only Unlock. **Debits · Finance Review** is a separate monthly tab; debit classifications remain editable GCash metadata and do not create a Finance expense. The summary retains imported, available, posted, remarked, and debit totals, and the Not for Posting tab badge shows the selected month's locked-credit count. **Match Use** shows assigned or suggested customer names and amounts, Available for unassigned credits, the lock detail for Not-for-Posting credits, and a dash for debits; account numbers, billing months, and immutable allocation details remain backend-only. Recipient `09361565251` displays as **Gcash - Archie** and `09651404623` as **Gcash - Frances** without replacing the original number. Bind & Post still supports one to three distinct current customer allocations whose total exactly equals the imported credit, including advance payments, while the server continues to derive the current billing month and enforce immutable assignment and duplicate-reference protections.
- `/gcash-transaction.html` now separates **Imported GCash Transactions** and **Pending GCash Payments** into Tabler tabs. The pending tab shows the manually selected customer, amount, date, entered reference, and Pending status. Bind Proof opens only exact amount/date imported candidates; a selected official row and confirmation are required before Verify & Post. `/payments.html` warns that GCash is saved as Pending, while Payment History and both Payment Breakdown views display the pending amount. In Payment Breakdown, **Amount Paid** shows only the numeric amount and **Payment Mode** carries `GCash · Pending`. Pending amounts stay separate from `amountPaid`, payment status, ending balance, service decisions, effective statements, collection reports, and reminder payment confirmations until verification succeeds.
- When an available imported row's reference, amount, date, and customer uniquely match one pending entry, its Actions column shows **Bind Pending** instead of generic **Bind & Post**. The shortcut preselects that official row in the existing **Verify Pending GCash Payment** modal. The modal is arranged into Pending Payment, Official Matches, Verification Summary, and Confirmation sections with a persistent action footer. Candidate cards show reference, exact time, sender, recipient, description, and amount; the live summary compares pending and official amount, date, reference, and recipient, marking exact values and items that need review. An exact-reference candidate is preselected, a lone candidate is selected automatically, and changing candidates clears the confirmation checkbox. Verify & Post remains disabled until one proof is selected and confirmed, updates the same entry, and relies on unchanged server revalidation and idempotent duplicate protection.
- Imported history is one statement month per page and defaults from the browser's current **Asia/Manila** month. The month selector includes the current month plus every stored transaction month, with Older/Newer controls. Within that month, **Credits** is the default reconciliation view, **Debits · Finance Review** is a separate view, and **Not for Posting** isolates locked incoming credits with a monthly count badge. Locked credits no longer appear in Credits, retain their remark/Admin/time audit, and expose only Unlock; unlocking returns the same official row to Credits as Available without creating a payment. Credit status/search filters never mix debit or locked rows into payment work; the debit view retains existing GCash classification metadata and explicitly creates no Finance expense. No imported row is moved, copied, or deleted, and a future Finance handoff must use the official GCash reference as its duplicate-prevention key.
- Pending-proof review behavior remains implemented in the unchanged browser/backend contracts, including official-history matching, **Approve & Post**, **Request New Proof**, and **Reject**, but the removed table no longer exposes those records or actions on this page. Gmail controls and Android phone-detection controls are not part of the queue.
- The retained approval dialog shows the stored screenshot fields, local OCR confidence, analyzer source, sanitized Vision AI status/model/confidence, and warnings beside the independent official-history result. OCR and AI remain advisory until the Admin explicitly approves.
- `/payment-confirmation-queue`, `/payment-confirmation-queue.html`, `/payment-confirmation-queue-history`, and `/payment-confirmation-queue-history.html` redirect authenticated Admins to `/gcash-transaction.html` for compatibility; the old HTML history implementation remains retired. Current UI behavior is implemented only from the canonical module web root.
- `/payment-history.html` reports how many official GCash references were auto-bound, linked to existing payments, already posted, conflicted, suggested, or reserved for retry after an Excel import. Its final server reconciliation also catches an official PDF imported at nearly the same time; conflict details remain in import warnings and no browser code decides whether a payment is safe to bind. The page loads the branch's posted binding IDs with its canonical payment records: unbound imported payments retain **Edit & Bind**, while an entry already posted in GCash Transactions shows a disabled lock action with the tooltip **Locked—this GCash transaction is already posted**. If binding status cannot load, imported Edit actions fail closed as disabled until refresh. Newly bound official-history payments display the imported GCash received date and time; the hidden timestamp token is never shown in Notes. Payment Breakdown displays the same official received calendar date but continues attaching the payment to the server-selected current billing cycle using the separate posting timestamp, so calculations do not move to an older statement month.

## Data and dependencies

- JSON is the default storage mode; optional relational paths use shared DB helpers.
- Prepaid accounts receive one idempotent monthly recurring charge on the first day of each Manila billing month; overdue prepaid cycles catch up in one scheduler run. Postpaid month-end scheduling is unchanged.
- A new postpaid customer's first mid-month activation period is calculated immediately as one prorated backend cycle. A payment recorded before the following postpaid month-end settles that activation cycle first; only an amount above the prorated charge becomes advance. Established postpaid recurring charges remain pending/view-only until the last day of the month.
- Multiple payments are credits against the existing monthly cycle. Excess credit becomes advance for the next cycle; deprecated per-payment `Prepaid renewal charge` debits remain in raw audit history but are excluded from effective balances and breakdown rows.
- When a disconnected account uses the Stop Billing policy, effective payments recorded after the final generated cycle settle that last frozen balance instead of being discarded or generating a new monthly bill. Partial payments leave the remainder unpaid, full payments settle it, and excess becomes advance.
- Effective-dated upgrades/downgrades are backend-owned. Unpaid cycles from the billing-effective month use the new canonical plan amount. Finalized paid rows retain their original amount and plan label; their prorated/full price difference is carried exactly once to the first open cycle as a separate debit or credit. The Billing adjustment store is the audit/history source, while Customer Management owns the subscriber's current plan and future scheduled snapshot. Current changes synchronize the resolved MikroTik profile immediately; future changes reuse the Billing scheduler's retry-safe scheduled profile application.
- Referral discounts are calculated only from active registry applications. Approval locks the entitlement without waiting for a referred-client payment. Billing creates exact-month automatic applications in FIFO order among records whose optional earliest `applyFromMonth` has arrived, at most two per cycle; unapplied and future-scheduled records remain queued indefinitely, and a reversed application skips its original month and returns to the later-month queue.
- Complimentary periods are stored in the branch/account Billing adjustment record. The scheduler advances prepaid first-of-month and postpaid month-end cycles without generating recurring charges for exempt months, while canonical rows retain any kept balance or apply the captured write-off once without creating advance. Active complimentary accounts stay service-eligible and are suppressed from automatic referral allocation, billing reminder queues, and credit-limit disconnection queues. A finite period resumes at the next valid plan cycle; an open-ended period has no next billable date.
- Reconnection settlements are stored as sanitized audit history on the branch/account disconnection decision. A stopped interval never creates catch-up cycles. The previous balance remains a separate ledger component and is either retained, written off once, or deferred into an exact cent-balanced schedule. Admin may create a separate remaining-days proration, a separate complete-plan full-month charge, or no immediate charge; either immediate charge advances the following prepaid first-day or postpaid month-end regular cycle so it cannot duplicate the selected month. Pending-payment settlements create no charge, count only new collected-payment entries, protect those activation payments from deletion, and rebuild the selected charge plus next billing/due dates from the actual activation day if payment arrives later.
- Payment confirmation proof paths remain rooted at shared `public/uploads` through `core/runtime/paths`.
- `backend/payment-confirmation-queue-store.js` supports the default JSON store key `payment_confirmation_queue` and optional MySQL table, including submitted payment date and SHA-256 screenshot hash. New submissions always start as `pending`; duplicate images and pending/approved references are rejected.
- Screenshot analysis metadata is stored as `proofAnalysis` in JSON and `proof_analysis_json` in MySQL. `backend/gcash-screenshot-parser.js` adapts the GCash label strategy from AJAbanto's MIT proof of concept to the Node runtime, runs Tesseract.js with packaged English trained data, serializes OCR work through one lazy worker, and caches recent image hashes to avoid duplicate recognition between preview and submission. Local OCR starts with a block-layout pass, supplements incomplete or low-confidence reads with automatic layout and then sparse-text recognition when key fields remain missing, combines the passes before parsing, normalizes a common `+163` OCR error to the Philippine `+63` number, and rejects status-bar text as a recipient name. `backend/gcash-vision-ai.js` is disabled by default; `GCASH_VISION_AI_ENABLED=true` plus an HTTP(S) `GCASH_VISION_AI_ENDPOINT` and `GCASH_VISION_AI_MODEL` opt into an OpenAI-compatible chat-completions endpoint, with optional `GCASH_VISION_AI_API_KEY`, provider label, timeout, and OCR-confidence threshold. It sends screenshots externally only after that opt-in, caches normalized results briefly, and never persists an endpoint, credential, raw provider response, or screenshot copy.
- Hybrid analysis never overwrites a locally read field. Vision AI fills only missing fields, records per-field `local_ocr`, `vision_ai`, or corroborated provenance, and raises visible conflict warnings when it disagrees. AI-only contradictions do not become an automatic approval blocker; imported history, locked customer submission fields, immutable transaction allocation, and explicit Admin approval remain authoritative.
- `backend/gcash-pdf-parser.js` uses `pdfjs-dist` to read both credit and debit text rows from the official encrypted statement layout. `backend/gcash-transaction-history-store.js` stores batch metadata, normalized transactions, audited debit classifications, active Not-for-Posting locks with append-only lock/unlock audit, and claimed/posted allocation groups in the storage-aware `gcash_transaction_history` app store, so JSON and MySQL modes share the same branch-scoped contract. Missing debit/credit values remain null rather than being converted to zero. PDF hashes identify repeat uploads, while normalized exact references are the branch-wide uniqueness key across every PDF. Existing references—including locked rows and rows whose incoming details differ—are retained unchanged and counted as skipped so unrelated new rows still import; a claimed or posted group cannot be reassigned to a different allocation signature. Each group retains one to three customer/account, exact amount, server-derived current billing month, payment-entry associations, current posting date, original imported transaction data, claiming Admin/time, and posted time; legacy single-allocation fields are sanitized into the same group representation. Recipient labels are derived for display without replacing the original number.
- `backend/payments.js` exposes the internal idempotent approved-payment writers used by proof approval and direct imported-history posting. The batch writer validates one to three distinct allocations totaling the imported credit, supports both JSON and relational payment storage, rejects normalized reference/OR-number reuse before insertion, assigns canonical entry numbers in relational transactions, records the Admin and GCash source on each entry, and commits all entries together before running existing paid-service/prepaid refresh effects. Identical retries return the complete stored group; partial pre-existing groups fail closed.
- `backend/payment-confirmations.js` keeps approval/rejection/new-proof requests staff-only. **Request New Proof** changes only the queue status to `needs_new_proof` with customer-visible instructions. For GCash, **Approve & Post** requires an independent imported-history match, immutable submitted amount/reference, no reliable OCR contradiction, and explicit Admin confirmation that the transaction belongs to the displayed customer/bill. The official transaction is claimed for exactly one submission/account, the idempotent ledger credit and queue review are posted, and the allocation is finalized with the payment entry; only that path triggers receipt data, prepaid extension, and paid-service/MikroTik refresh behavior.
- Payment backups remain rooted at `data/payment-backups`, and Cloudflared configuration remains rooted at `.cloudflared/config.yml`.
- Depends on Customer Management for customer identity, plan assignment, referral state, and archive visibility.
- Depends directly on the migrated Network backend for RouterOS connectivity, PPPoE normalization/auditing, and service enforcement; Network consumes Billing plan-profile intent.
- Collector submits payments, approvals, and remittances against Billing records.
- Customer App submits payment confirmations and displays customer balances/statements.
- Business profile and integration settings are Admin-owned shared inputs.
- Payments, Payment History, Payment Breakdown, and the Customer Management customer list require `/api/payment-records` as their shared billing read source. Missing/invalid summaries display a Billing unavailable state; browser billing fallbacks are not executed.
- The Payments breakdown table remains display-only: it formats version 2 `billingSummary.rows` but does not calculate charges, balances, advances, or statuses in the browser. Its **Add Payment** action reuses the existing locked-customer payment form and `/api/payments/:accountNumber` write path; adjustment, plan-change, and service controls remain on the existing full page.
- Manual Billing payments already pass through `/api/payments/:accountNumber`, which validates duplicate references/fingerprints and uses a database transaction in relational mode. Collector capture remains a separate Collector-owned write contract and is not changed by this read-model cutover.
- Payment History GCash reconciliation uses the official history store's normalized branch reference and immutable allocation claim/finalize contract. The same canonical service runs after either PDF or Excel import, requires one to three distinct current customers plus exact normalized reference, GCash direction, total, and calendar date, and finalizes against existing payment entry IDs. Review suggestions are derived at read time and never reserve or write a payment. New matching entries keep the existing Excel-import ledger shape and service-refresh behavior; the workflow does not call the direct current-month posting writer and does not recalculate or edit Payment Breakdown rows.
- `/payment-history.html` exposes a small Edit icon only for imported Cash/GCash rows. Its Tabler modal keeps amount and date read-only, searches all current branch clients (including paid clients) with name-and-amount-due suggestions, shows the current official binding, and lists unused exact amount-and-date credits as replacement choices. The current reference remains selected for client-only corrections; choosing another reference clearly warns that the previous single-payment binding will be released. Close and Cancel always invoke the modal close path independently of save eligibility. The confirmation-gated save refreshes the canonical Payment History read model; internal edit and reference-reassignment audits remain hidden from the row note.

## Known risks and follow-up

- Billing and payment files are large and contain cross-module network/service effects; behavior changes require focused financial regression coverage.
- Scheduler changes can create duplicate or incorrectly timed charges; test Manila-time and idempotency behavior.
- Payment numbering and relational transactions are high-integrity paths.
- Shared receipt, statement, route-mount, and static-delivery logic in `server.js` requires Integration Codex locks.
- Repository-root backend aliases must not be recreated; scripts and cross-module consumers use canonical paths.
- Expand automated billing-cycle, relational transaction, and disconnection-policy tests before changing behavior.
- The imported email PDF is stronger independent evidence than a screenshot but is not a live GCash settlement API. Admins must verify that the statement email/file is genuine and still make the final approval decision; no screenshot or parsed statement row automatically posts a ledger credit.
- OCR accuracy depends on screenshot resolution and GCash layout changes. Missing or conflicting fields are warnings and never silently become confirmation; the customer may still submit unreadable evidence for manual review.
- Enabling Vision AI sends unclear customer screenshots to the configured external provider. The operator must choose an appropriate provider, protect its API key, review retention/privacy terms, and keep the fallback disabled when external processing is not acceptable.
- The debit view is only a Billing-owned review surface. Creating Finance expenses from selected debits, recording a cross-module handoff audit, and enforcing reference-based Finance idempotency remain future Finance/Integration work.

## Validation

- `npm run refactor:billing` verifies the descriptor, retirement of thirteen root entries, 36 web files, server wiring, repository-root data paths, and representative normalization/profile/balance behavior.
- `npm run refactor:billing` also verifies both breakdown views load the shared table renderer, Payments no longer navigates away on subscriber/row selection, the modal requests the per-account canonical API, and a representative prepaid row renders through the shared component.
- `node Features/modules/billing/tests/prepaid-billing-cycle.test.js` covers first-of-month prepaid dates, same-month payment consolidation, legacy debit exclusion, advance carry-over, post-stop settlement against the final frozen balance without a new bill, and unchanged postpaid month-end rows.
- `node Features/modules/billing/tests/canonical-billing-summary.test.js` verifies version 2 of the backend summary, same-month payment consolidation, unchanged pre-month-end recurring postpaid representation, postpaid activation-proration settlement, reconciliation detection, and backend-only consumption by all four pages.
- `node Features/modules/billing/tests/effective-plan-change.test.js` verifies paid-row amount/label preservation, unpaid forward recalculation, one-time upgrade debit, one-time downgrade credit/advance, audited plan history, and safe prepaid/postpaid transition rejection. It is also executed by `npm run refactor:billing`.
- `node Features/modules/billing/tests/referral-application.test.js` verifies that an approved referral needs no referred-client payment and changes only the exact month recorded in its automatic application; unapplied and wrong-month referrals do not change due amounts. It is also executed by `npm run refactor:billing`.
- `node Features/modules/billing/tests/complimentary-account.test.js` verifies period boundaries, retained balances, audited write-off without advance, zero recurring charge, unchanged plan identity, canonical Complimentary status, postpaid resume timing, reconciliation behavior, and reminder-queue suppression. It is also executed by `npm run refactor:billing`.
- `node Features/modules/billing/tests/reconnection-settlement.test.js` verifies stopped-cycle suppression, postpaid proration, prepaid/postpaid full-month charges, following-cycle advancement, retained/write-off/installment previous balances, exact installment cents, separate next-cycle rows, no pending-payment charge, activation progress, and delayed-payment restart from the actual service date. It is also executed by `npm run refactor:billing`.
- `node Features/modules/billing/tests/customer-payment-proof.test.js` verifies proof image type/signature validation, GCash OCR field extraction/checks, noisy status-bar and `+163` phone normalization, supplemental-pass decisions, Vision AI normalization/activation decisions, hybrid field provenance and sanitization, reference normalization, customer-session analysis/manual-review route contracts, persisted-analysis schema, and protected customer/Admin page wiring.
- `node Features/modules/billing/tests/gcash-transaction-history.test.js` verifies official statement row parsing, reference/credit/date/recipient matching, sender/customer warnings, in-memory claim/finalize persistence, wrong-customer reuse rejection, importer/allocation UI wiring, the empty-proof explanation, the manual approval gate, and proof-gated manual GCash capture. Pending entries stay financially ineffective; exact date/amount options bind the official row to the same entry ID, adopt its reference/time, and retry idempotently without adding a duplicate ledger row. It is also executed by `npm run refactor:billing`.
- The GCash history test additionally verifies three-account allocation claims/finalization, server-derived current-month posting, ending-balance caps, exact-total and distinct-account guards, atomic JSON batch persistence, relational transaction insertion with one intentionally shared reference and separate OR-number checks, group idempotency, conflict rejection, and the rendered Add/Remove allocation UI contract without a month picker.
- 2026-08-16 live browser validation confirmed the Imported/Pending Tabler tabs, empty Pending state, GCash form warning, and `Save Pending GCash` action with no payment submission or data mutation; the page produced no console errors.
- `npm run refactor:phase5` runs inventory, Core, Admin, Customer Management, Billing, security, and isolated HTTP checks.
- `npm run refactor:phase12` is the final cross-module structural, module, integration, security, HTTP, and package gate.
- The HTTP suite covers unchanged Billing asset/page URLs, authentication boundaries, the public plan API, and unauthenticated API denial on ports `3190`/`4190`.
- 2026-08-14 flavor-retirement validation covered authenticated queue access, retired flavor page/API `404` behavior, unchanged queue records/actions, syntax, focused proof tests, Billing compatibility, the complete `npm test` Phase 12 gate, and live browser rendering with no console errors.
- 2026-08-13 queue-table removal validation covered the absence of pending-proof table/body/footer markup, retention of the Payment Confirmation Queue title and Imported GCash Transactions table, unchanged browser/backend approval contracts, HTML structure, JavaScript syntax, `git diff --check`, focused proof/history tests, and `npm run refactor:billing`.
- 2026-08-13 direct imported-GCash posting validation covers normalized duplicate rows, both requested recipient labels, exact imported-amount rejection, account/month allocation persistence, wrong-cycle reassignment rejection, one-time posting plus idempotent retry, static Admin UI/API wiring, JavaScript/backend syntax, focused proof/history tests, `git diff --check`, `npm run refactor:billing`, and the full `npm test` Phase 12 gate.
- 2026-08-14 Payment Confirmation Queue Tabler validation covered unique HTML IDs/structure, focused GCash history and customer-proof tests, `git diff --check`, `npm run refactor:billing`, and rendered desktop/mobile checks of the imported-history card plus Import and Bind & Post dialogs with no browser console warnings or errors.
- 2026-08-14 GCash bulk-import validation covered normalized duplicate references within one PDF and across prior branch history, differing details on an existing reference, mixed new/existing batches, repeat-file idempotency, retained canonical values, syntax, focused history tests, `git diff --check`, and `npm run refactor:billing`.
- 2026-08-14 GCash remark validation covered debit-row PDF parsing/import, null direction-value preservation, automatic Expense — Unclassified metadata, audited Admin edits, invalid-category rejection, unchanged source transaction fields, route/UI wiring, rendered remark interaction, focused proof/history tests, `git diff --check`, and `npm run refactor:billing`.
- 2026-08-14 GCash helper-text removal validation covered the absence of the classification prompt, retained saved-remark audit rendering, updated browser asset version, JavaScript syntax, the focused GCash history test, `git diff --check`, and `npm run refactor:billing`.
- 2026-08-14 GCash Description-column validation covered parsed credit/debit descriptions, one Description header/cell, retention of the combined Amount column, responsive table width, cache-busted assets, JavaScript syntax, the focused GCash history test, `git diff --check`, and `npm run refactor:billing`.
- 2026-08-14 GCash action-column validation covered removal of the separate Remark header/cell, credit-only Bind & Post/locked status controls, compact debit-only Remark/Save controls, JavaScript syntax, focused Billing tests, `git diff --check`, `npm run refactor:billing`, and rendered browser interaction with no console errors.
- 2026-08-14 Payment Queue Tabler-refinement validation covered summary counts/amounts, client-side search and status filters, no-match handling, unchanged row actions/modals, responsive CSS and desktop rendering, focused Billing tests, `git diff --check`, `npm run refactor:billing`, and a clean browser console.
- 2026-08-14 compact imported-table validation covered customer-name/amount-only Match Use rendering, Available/dash fallbacks, eight-column fixed proportions, compact debit action controls, unchanged posting state, focused Billing tests, `git diff --check`, `npm run refactor:billing`, and live desktop rendering with a clean browser console.
- 2026-08-14 current-month imported-payment validation covered server-derived Manila month/date, canonical ending-balance caps, an account-and-amount-only browser request, no Open Billing Month picker, three read-only August 2026 targets, successful atomic posting, focused proof/history tests, Billing compatibility, and a clean browser console. Payment Breakdown source and calculation files were not changed.
- 2026-08-14 GCash client-autocomplete validation covers client-name and account-number filtering, name-and-amount-only suggestions and selected values, mouse and keyboard selection, duplicate-account exclusion across allocation rows, absence of visible billing-month controls and account/month metadata, retained server-derived current-month and ending-balance behavior, focused static regression checks, and rendered browser verification.
- 2026-08-14 GCash advance-payment validation covers paid/zero-balance client visibility, explicit **Advance Payment** suggestion/selected/summary labels, uncapped advance allocation, retained unpaid amount-due cap, server-derived current-month posting, focused route/UI regression tests, Billing compatibility, and rendered browser verification with a clean console.
- 2026-08-14 compact queue-header validation covered the short GCash Reconciliation header, one inline four-metric summary bar, small header and icon-only row actions, removal of the verification banner and redundant batch filename/count metadata, unchanged API/record contracts, JavaScript syntax, focused Billing tests, `git diff --check`, `npm run refactor:billing`, the full `npm test` gate, and authenticated browser rendering.
- 2026-08-14 Payment History GCash-reference reconciliation validation covered normalized one- and two-account planning, exact amount/date guards, new-payment auto-binding, existing-ledger binding without reinsertion, idempotent repeat imports, immutable assignment finalization, conflict reporting text, backend/browser syntax, focused proof/history tests, `git diff --check`, `npm run refactor:billing`, and the full `npm test` gate.
- 2026-08-15 GCash page-retirement validation covered the renamed HTML entry point and title, updated protected route/sidebar/module manifest, old-page compatibility redirects, retired history HTML/JavaScript/CSS, focused proof/history contracts, Billing compatibility, HTTP smoke coverage, context validation, and the full `npm test` gate.
- 2026-08-15 order-independent GCash reconciliation validation covers Payment-History-first exact auto-binding, existing-entry ID finalization without a second ledger write, amount-mismatch suggestions without assignment, suggestion search/render contracts, backend/browser syntax, focused history tests, `git diff --check`, Billing compatibility, and the full `npm test` gate.
- 2026-08-15 leading-zero reference validation covers the real `43891500420` / `0043891500420` pair, exact-first resolution, ambiguous padded-reference rejection, repair of a legacy short ledger reference without reinsertion, future official-reference preservation, refresh-time reconciliation wiring, focused history tests, syntax, `git diff --check`, Billing compatibility, and the full `npm test` gate.
- 2026-08-15 Payment History Edit & Bind validation covers exact available-credit option filtering, Admin-confirmed relocation of one existing imported entry without changing its ID/amount/date or increasing ledger count, source-account removal, target-account insertion, official-reference/method update, hidden audit metadata, immutable GCash finalization, idempotent retry, backend/browser syntax, static Tabler modal contracts, `git diff --check`, Billing compatibility, and the full `npm test` gate. Direct local-file rendering was not attempted beyond the browser security boundary; the running port-3000 deployment was left untouched.
- 2026-08-15 Payment History posted-binding-lock validation covers the branch-scoped bulk binding list, disabled lock action and tooltip, fail-closed UI behavior when binding status is unavailable, backend rejection of both option reads and saves for posted entries, preservation of the original customer/reference/amount/date and ledger count when a replacement is attempted, focused GCash history tests, syntax, `git diff --check`, Billing compatibility, and the full `npm test` gate.
- 2026-08-15 official GCash received-time validation covers normalized Manila transaction timestamps, JSON and relational hidden timestamp persistence, official date/time display selection in Payment History, hidden metadata removal from Notes, official Payment Breakdown date display, current-cycle allocation based on the separate posting time, preserved Admin/assignment posting audit timestamps, focused GCash history tests, syntax, `git diff --check`, Billing compatibility, and the full `npm test` gate.
- 2026-08-16 GCash Not-for-Posting validation covers required remark validation, incoming/unassigned-only locking, Admin/time and append-only unlock audit, replay safety, branch isolation, immutable imported fields, retained duplicate-reference protection, shared claim rejection, direct-post rejection, Excel reconciliation conflict/skip behavior, existing-ledger auto-bind exclusion, Edit & Bind exclusion, static route/UI contracts, focused history tests, syntax, Billing compatibility, context/diff checks, full `npm test`, and authenticated browser rendering/open-close without mutating a live transaction.
- 2026-08-16 New Transaction customer-search validation covers name/account filtering, name-and-amount-due suggestions, hidden canonical account selection, mouse and Arrow/Enter selection, existing amount autofill, locked-customer flows, JavaScript syntax, Billing compatibility, the full `npm test` gate, browser rendering, a clean console, and closing without a payment submission.
- 2026-08-16 pending-amount display validation covers the shared Payment Breakdown renderer in both entry points, amount-only pending lines under Amount Paid, retained `GCash · Pending` mode status, unchanged financial calculations, Billing compatibility, the full `npm test` gate, browser rendering, and a clean console.

- 2026-08-16 manual-reference reuse validation covers branch-wide Payment History and pending-entry conflicts, imported-GCash conflicts, case/space/hyphen normalization, numeric leading-zero equivalence, inline Payments feedback, authoritative pre-write rejection, backend/browser syntax, focused GCash history tests, Billing compatibility, the full `npm test` gate, context/diff checks, and authenticated browser rendering without submitting a payment.

- 2026-08-24 imported-row pending-binding validation covers exact pending-match serialization, the visible **Bind Pending** action, selected-reference prefill, distinguishable multi-candidate details, same-entry/no-duplicate guidance, candidate-change reconfirmation, stale-match rejection, unchanged confirmation/server gates, backend/browser syntax, the focused GCash history test, Billing compatibility, and diff/context checks.
- 2026-08-24 monthly GCash history validation covers validated month-scoped reads, branch totals and available-month metadata, current-Manila-month browser defaulting, Older/Newer month navigation, separated Credit/Debit views, Finance non-creation guidance, retained debit classifications, browser/backend syntax, focused history tests, Billing compatibility, and diff/context checks.
- 2026-08-24 Not-for-Posting tab validation covers monthly locked-credit counts, exclusion from the Credits view, isolated remark/Admin/time display, Unlock-only actions, return to Available after unlock, browser syntax, focused history tests, Billing compatibility, and diff/context checks.
- 2026-08-24 pending-verification modal validation covers the four-section layout, responsive official-match cards, live amount/date/reference/recipient comparison, confirmation reset on candidate changes, persistent duplicate-safety guidance, unchanged submit gate, browser syntax, focused history tests, Billing compatibility, and diff/context checks.

## Latest meaningful changes

- 2026-08-24: Reorganized **Verify Pending GCash Payment** into four review sections with selectable official transaction cards, a live comparison summary, explicit confirmation, and a stable action footer. The modal still updates the same pending entry only after selection and confirmation; server-side exact-match and duplicate protections are unchanged.

- 2026-08-24: Added **Not for Posting** as a third monthly GCash transaction tab with a month-specific count. Locked credits are excluded from Credits, keep their classification and lock audit, and offer only Unlock; unlocking returns the same official transaction to Credits as Available and never creates or duplicates a payment.

- 2026-08-24: Changed Imported GCash Transactions to one statement month per page, defaulting to the current Manila month and exposing available-month plus Older/Newer navigation. Credits remain the default payment-reconciliation view; debits are separated into **Debits · Finance Review**, retain their existing GCash classification metadata, and do not create Finance expenses or duplicate imported rows.

- 2026-08-24: Connected imported **Match Use** suggestions to the existing pending-payment binding workflow. A unique exact same-reference/customer/amount/date match now shows **Bind Pending** in Actions and opens the same-entry confirmation workflow; generic direct posting is no longer offered for that matched row. When multiple credits share the pending amount/date, the dialog exposes their reference, time, sender, recipient, description, and amount, preselects the exact-reference row, and explicitly confirms that Verify & Post updates the pending entry without creating a duplicate payment.

- 2026-08-16: Prevented Payments **New Transaction** from accepting a reference already present in branch Payment History, pending GCash entries, or Imported GCash Transactions. The browser checks while entering and before submit, while JSON/MySQL writes repeat the normalized check server-side; numeric leading-zero variants are treated as the same reference. Existing payment, imported transaction, binding, and balance records are unchanged.

- 2026-08-16: Simplified pending GCash display in both Payment Breakdown views. **Amount Paid** now shows only the pending amount; the Pending label remains under **Payment Mode** as `GCash · Pending`. Storage, posting gates, amount-paid calculations, statuses, and balances are unchanged.

- 2026-08-16: Replaced the Payments **New Transaction** customer dropdown with a searchable autocomplete. Admin can type a client name or account number, view client-name and amount-due suggestions, and select by mouse or keyboard; the existing canonical account-number payload, amount autofill, locked-customer launch, and payment posting behavior remain unchanged.

- 2026-08-16: Added proof-gated manual GCash capture. Payments saves Admin-entered GCash as `pending_gcash_verification`; GCash Transactions provides a Pending tab and exact imported-proof binding; Payment History and Payment Breakdown show Pending without changing balances. Verification updates the same entry ID with the official reference/date/time and permanently finalizes the imported assignment, with idempotent retry and no duplicate ledger row.

- 2026-08-16: Added Admin **Remark & Lock** for imported incoming GCash credits that belong to no customer account. A required remark marks the retained official row **Not for Posting**, records Admin/date/time, removes every posting and auto-binding path at the shared claim layer, keeps duplicate-reference protection, and supports audited Admin unlock without changing reference, direction, amount, description, date, recipient, allocations, Payment History, or Payment Breakdown calculations.

- 2026-08-16: Added a canonical, duplicate-safe first-bill adjustment transfer helper for customer-focused export/import. Previous Balance and Advance round-trip by account in JSON or MySQL mode; legacy field aliases migrate into `payment_breakdown_adjustments`, while existing newer fields and all unrelated Billing adjustment history remain unchanged.

- 2026-08-15: Newly bound GCash Transactions now use the official imported transaction date and time as the customer-facing payment timestamp. Payment History shows the official date and time, and Payment Breakdown shows its official calendar date. The actual Admin posting time remains separate in ledger `recordedAt` and assignment `postedAt`; current-cycle allocation still uses that posting time, so no Payment Breakdown calculation or historical bill row is moved. Existing bound records are not rewritten automatically.
- 2026-08-15: Locked Payment History **Edit** after an imported payment is posted to an official GCash transaction. A branch-scoped read endpoint supplies posted payment entry IDs so bound rows render a disabled lock icon and tooltip; the edit-option and save APIs enforce the same rule against stale or direct requests. Unbound imported payments remain editable and bindable, binding-status failures disable editing, and rejected attempts leave the customer, reference, amount, date, payment count, official assignment, and Payment Breakdown calculations unchanged.
- 2026-08-15: Added a guarded Payment History **Edit & Bind** modal and APIs for correcting one imported Cash/GCash row to the right current client and selecting its exact available official GCash credit. Amount/date stay locked, the same entry ID is moved rather than duplicated, leading-zero-safe reused references and already-posted assignments fail closed, JSON/MySQL writes preserve financial fields and store an Admin audit token, and the official transaction is finalized against the existing payment entry. Payment Breakdown computation remains unchanged.
- 2026-08-15: Added exact-first handling for numeric GCash references whose leading zeros were removed by Excel. A unique padded/short pair can bind only with the existing exact amount/date/current-customer/GCash-payment guards; ambiguous official candidates remain conflicts. Existing payment history is not rewritten or duplicated, future matched imports retain the official padded reference, and refreshing GCash Transactions repairs safe legacy matches. Payment Breakdown calculations are unchanged.
- 2026-08-15: Made official GCash PDF and Payment History Excel reconciliation order-independent, including near-simultaneous imports. Exact normalized reference/GCash total/date/current-customer groups link the already-recorded payment entry IDs and permanently lock the imported reference without inserting another payment. Non-exact same-reference rows stay unbound and show only suggested current-customer names and amounts for Admin review. Payment Breakdown calculations and historical billing rows are unchanged.
- 2026-08-15: Renamed the active imported-statement page and sidebar entry to **GCash Transactions** at `/gcash-transaction.html`. The old Payment Confirmation Queue URLs redirect to the new page, while the unused standalone Queue History page and its dedicated JavaScript/CSS/routes were retired. Backend proof, imported transaction, allocation, payment, and audit records remain unchanged.
- 2026-08-14: Payment History Excel imports now reconcile normalized GCash references with Imported GCash Transactions. Exact incoming-credit customer/amount/date groups are claimed and auto-bound to their imported ledger entries, exact payments already present are linked without duplication, identical posted references are skipped, and conflicting or reserved references remain unchanged for Admin review. Manual unmatched-row binding uses the same guard; Payment Breakdown calculations are unchanged.
- 2026-08-14: Compacted Payment Confirmation Queue into a short **GCash Reconciliation** header, one inline summary bar, small Refresh/Import and icon-only row buttons, and a latest-import-only history subtitle. Removed the redundant verification banner without changing imported records, payment posting, remarks, or billing calculations.
- 2026-08-14: Removed deployment feature gates from Billing pages and APIs. Payment Confirmation Queue and Queue History now follow normal Admin authentication in every deployment, while proof review, imported-reference uniqueness, immutable allocations, ledger posting, and Payment Breakdown calculations remain unchanged.
- 2026-08-14: Simplified imported GCash **Match Use** to assigned customer name and amount only, moved assignment state entirely to Actions, and compacted all eight table columns with controlled wrapping so the table fits its desktop card more cleanly. Stored allocation records and calculations are unchanged.
- 2026-08-14: Added responsive Tabler summary cards and an imported-transaction search/status toolbar to Payment Confirmation Queue. Filtering is client-side and never changes official rows, allocation locks, posting behavior, remarks, or billing calculations.
- 2026-08-14: Moved the imported GCash debit Remark selector and compact Save button into Actions, removed the separate Remark column, and limited credit Actions to Bind & Post or the locked assignment status.

- 2026-08-14: Included paid clients in imported GCash Customer Account search for advance payments. Paid/settled or zero-balance clients display **Advance Payment** and accept a positive allocation through the existing current-cycle ledger calculation; unpaid clients still show and cannot exceed their amount due, and historical Payment Breakdown rows/calculations remain unchanged.
- 2026-08-14: Simplified each imported GCash Customer Account autocomplete so its suggestions and selected value show only the client name and amount due. Name/account-number filtering and duplicate-account exclusion remain available without exposing the account number or month in the interface.
- 2026-08-14: Removed the visible current-billing-month field from imported GCash **Bind & Post**. The server still derives the canonical current month, uses the canonical ending balance as the payment maximum, and records on the current Manila date so only the current Payment Breakdown row receives the payment; prior rows and all breakdown calculations remain unchanged.
- 2026-08-14: Extended imported GCash **Bind & Post** to split one incoming credit across one to three distinct customer accounts. The exact-total gate, immutable group claim, atomic JSON/MySQL ledger write, shared reference, per-entry audit, and idempotent retry prevent partial or duplicate posting.
- 2026-08-14: Added the exact imported GCash transaction Description as its own table column while retaining one combined Amount column with Credit/Debit direction.
- 2026-08-14: Removed the unsaved classification helper sentence from imported GCash rows; selectors and Save actions remain, while saved remarks still show their Admin/time audit metadata.
- 2026-08-14: Added audited editable remarks to every imported GCash row. Debit transactions are retained with their actual debit amount, default to Expense — Unclassified, and cannot be posted as customer payments; Admins can reclassify any row without altering its official transaction data.
- 2026-08-14: Made GCash PDF imports continue through all valid rows. Every normalized reference already stored or previously encountered in the upload is skipped and counted—even when its incoming details differ—while all other new references import; repeat files with no new references succeed without creating duplicate batch history.
- 2026-08-14: Reworked the visible Payment Confirmation Queue as a responsive Tabler page: a clear reconciliation header, grouped refresh/import actions, one card-table for imported transactions, explicit verification guidance, and Tabler Import, Bind & Post, and duplicate-reference dialogs. Removed the empty legacy account filter/page-size panel while preserving all existing IDs, imported-history behavior, posting safeguards, and backend contracts.
- 2026-08-13: Added explicit Admin **Bind & Post** for available imported GCash credits. Admin selects a canonical branch customer and open billing month, verifies the read-only reference/recipient/exact credit, then posts one idempotent JSON/MySQL payment; the transaction becomes permanently Assigned and Posted with full allocation audit. Normalized duplicate references remain single-row across PDF imports, and the two configured recipient numbers display as Gcash - Archie and Gcash - Frances.
- 2026-08-13: Removed only the pending customer-proof table and pagination footer from Payment Confirmation Queue. The page title, GCash history import controls, Imported GCash Transactions table, backend queue data, APIs, and stored submissions remain unchanged.
- 2026-08-12: Fixed stopped-billing payment reconciliation. Effective payments recorded after the disconnection cutoff now reduce the final frozen billing row, update its payment details/status and canonical ending balance, and never create a post-disconnection monthly charge. Evelyn Sante account `100000217` now resolves seven January-through-July rows with the August Collector payment settling the July ₱3,000 balance to ₱0.
- 2026-08-11: Removed the Gmail GCash history automation and Android GCash notification bridge, including their routes, scheduler, stores, queue controls, screenshot-match coupling, and focused tests. Manual official-history PDF import remains the sole transaction-record match source; every customer proof remains Pending Review until explicit Admin **Approve & Post**.
- 2026-08-11: Improved local GCash screenshot extraction with block, automatic-layout, and sparse-text OCR passes. Readable reference/date fields missed by one layout are combined before parsing, common `+163` phone OCR is normalized, and Android status-bar garbage is no longer displayed as the recipient name; manual review and official-history approval gates are unchanged.
- 2026-08-10: Added an optional privacy-gated Vision AI fallback for incomplete or low-confidence GCash OCR. The OpenAI-compatible adapter is disabled until endpoint/model configuration is explicit, fills only missing fields, stores sanitized provenance/status, exposes its source in customer/Admin review, and cannot bypass official-history matching or manual approval.
- 2026-08-10: Bound each imported GCash transaction to one immutable customer payment allocation. Approval now locks submitted GCash amount/reference, blocks reliable OCR contradictions, requires explicit Admin customer-and-bill confirmation, flags sender/customer-mobile mismatch, claims the official reference before posting, and finalizes it with the ledger entry so another account cannot reuse it.
- 2026-08-10: Added local server-side GCash screenshot OCR based on the AJAbanto parser's label strategy. Customer previews and final submissions extract amount, reference, date/time, recipient, and status, compare them with the invoice, configured merchant, and imported history, store the sanitized analysis for Admin review, and remain Pending Review until explicit approval.
- 2026-08-10: Fixed the apparently empty Payment Confirmation Queue after a successful history import. The page now loads and displays imported statement transactions separately from pending customer proofs, reports the last import and row/batch counts, refreshes both views after import, and explains that imported rows are matching evidence rather than approval-ready customer submissions.
- 2026-08-10: Added Admin import of encrypted official GCash Transaction History PDFs, branch-scoped normalized history and duplicate protection, queue match badges, and a fail-closed GCash approval gate. Passwords and PDFs are not retained, imported matches never auto-approve, and the existing ledger/receipt/MikroTik path still runs only after explicit Admin approval.
- 2026-08-10: Made the payment-confirmation queue functional in JSON and MySQL modes, stored payment dates and screenshot SHA-256 hashes, rejected duplicate screenshots/references, prefilled submitted references for Admin review, and added idempotent JSON ledger posting with existing service-restoration hooks only after Admin approval.
- 2026-08-07: Added Admin-selectable **Full Month Now** to stopped-billing reconnection. It creates a separate canonical charge for the complete current plan amount, advances the next prepaid/postpaid regular cycle to prevent duplication, is blocked when the current month already has a generated bill, and retains the existing reason, confirmation, balance-treatment, and payment-gated audit safeguards. Temp is unchanged.
- 2026-08-07: Replaced direct stopped-billing reconnection with an audited settlement workflow. Admins choose Keep/Write Off/Installment for the previous balance, Prorated Now/Full Month Now/Next Regular Cycle for new billing, and Immediate/After Required Payment activation; stopped months are never back-billed, pending requests create no charges, delayed activation moves the new cycle to the actual service date, and activation payments cannot be deleted. Continue-billing disconnections preserve their existing cycle. Postpaid remains month-end and Temp is unchanged.
- 2026-08-07: Added audited Complimentary Accounts without creating a zero-price plan. Current/future exemptions preserve the subscriber's real plan and all historical records, suppress recurring charges, automated billing reminders, referral consumption, and credit-limit disconnection while keeping service active; Admins may retain or write off the current balance and may set an end/resume month without back-billing free periods. Payments and Payment Breakdown display the policy and history; postpaid remains month-end and Temp is unchanged.
- 2026-08-07: Fixed Payment Breakdown modal input by placing its two form modals above the shared Bootstrap backdrop and explicitly restoring pointer events on the dialog, content, form, body, and footer. The CSS asset version was bumped so browsers do not retain the blocked layer order.
- 2026-08-07: Replaced the inline first-bill adjustment and plan-change toolbars with separate responsive Tabler modals. Existing backend calculations, validation, paid-cycle confirmation, audit history, and referral controls remain unchanged.
- 2026-08-07: Added a read-only Approved Referral Queue to the full Payment Breakdown page so approved discounts are visible before Billing applies them. It shows the referred subscriber, locked amount, queued/requeued state, and earliest month while preserving automatic application and existing balance rules.
- 2026-08-07: Billing now honors Customer Management's optional referral `applyFromMonth` as the earliest eligible generated unpaid cycle. Blank remains next available; future referrals wait, while the existing two-per-month cap and automatic carryover continue unchanged.
- 2026-08-07: Added automatic unlimited referral carryover. Canonical payment records apply the two oldest queued Admin-approved discounts to the latest generated unpaid month, cap the value at the remaining charge, carry excess forward, and requeue reversals for a later month; manual Apply was removed.
- 2026-08-07: Replaced arbitrary referral amount adjustments with backend-owned, audited referral applications and reasoned reversals, preventing duplicate and month-mismatched use.
- 2026-08-07: Replaced browser-authored plan adjustments with an audited backend effective-plan workflow. Past unpaid cycles recalculate forward, paid rows remain immutable with one debit/credit carry adjustment, the actual subscriber plan and MikroTik profile follow the current effective plan, future changes use the existing retry-safe schedule, and the full Payment Breakdown page displays plan history. Plan-type transitions must still be reviewed in the subscriber editor; Temp is unchanged.
- 2026-08-07: Standardized Billing modal close controls on Payments, Disconnections, and Payment Confirmation Queue as shared Tabler outline-secondary icon buttons; modal behavior is unchanged.
- 2026-08-06: Changed Payments subscriber and row selection to open a responsive Payment Breakdown modal backed by the latest canonical per-account billing summary. Its presentation hides Type, Billing Cycle, and the plan/amount/source metadata beneath each Bill, then redistributes a viewport-wide compact fixed layout across the remaining 10 columns with whole-word header wrapping; narrow screens scroll horizontally. **Add Payment** now opens the existing locked-subscriber transaction form above the breakdown and refreshes the canonical rows after success. The modal and retained full page share one table renderer; the full page still shows all fields and keeps adjustment and service controls available through **Open Full Page**.
- 2026-08-06: Preserved a postpaid customer's stored first billing cycle when the first payment arrives later, represented a mid-month activation as one prorated backend cycle, and applied payments to that charge before classifying any excess as advance. Normal recurring postpaid generation remains month-end only; Temp is unchanged.
- 2026-08-06: Cut Payments, Payment History, Payment Breakdown, and Customers over to required backend-only `billingSummary` version 2 results. The backend now owns status/due-date calculations and returns per-account reconciliation plus an Admin branch report; postpaid remains month-end only and Temp remains unchanged.
- 2026-08-06: Prepaid synthetic breakdowns now retain an earlier stored billing-cycle month when the first payment is recorded later, preventing prior unpaid cycles from disappearing and being misclassified as advance. Postpaid and Temp calculations are unchanged.
- 2026-08-05: Prepaid Billing Cycle displays now show the current first-of-month cycle with Paid/Unpaid status and the next first-of-month cycle; service-expiration wording was removed from these displays. Postpaid and the isolated Temp module remain unchanged.
- 2026-08-05: Replaced prepaid per-payment renewal debits with one first-of-month monthly cycle, added catch-up/idempotent scheduler generation, excluded legacy renewal debits from effective calculations, consolidated same-cycle payments, and carried excess forward as advance without changing postpaid month-end billing.
- 2026-07-29: Phase 12 revalidated Billing through the canonical runtime and final package gate; no owned behavior, API, or UI contract changed.
- 2026-07-29: Phase 11 retired all thirteen Billing root shims and moved shared scripts plus Admin/Customer Management consumers to canonical Billing imports.
- 2026-07-29: Physically migrated thirteen backend implementations and 34 browser files into the Billing module, added root compatibility shims and module-loader/static wiring, preserved repository-root storage/config paths, and added Phase 5 compatibility and HTTP coverage.
- 2026-07-29: Phase 6 replaced temporary root Network imports with canonical Network backend dependencies in scheduler, disconnection, and payment flows.

## Context update rule

Update this file in the same task whenever owned behavior, APIs, data structures, UI workflow, tests, risks, dependencies, or source ownership changes.
